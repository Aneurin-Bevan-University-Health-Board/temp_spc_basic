# pip.conf — configures pip to check Test PyPI for abspc
# Place this file in your project root or at ~/.pip/pip.conf
#
# To activate project-local: set PIP_CONFIG_FILE=pip.conf before running pip
# Example: PIP_CONFIG_FILE=pip.conf pip install -r requirements.txt

[global]
extra-index-url =
    https://test.pypi.org/simple/
    https://pypi.org/simple/
